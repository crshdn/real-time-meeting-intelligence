# Real-Time Sales Assistant Environment Variables

# Deepgram API Key (required)
# Get from: https://console.deepgram.com/
DEEPGRAM_API_KEY=your-deepgram-api-key

# Anthropic API Key (required)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# WebSocket Server Port (optional, default: 8765)
WEBSOCKET_PORT=8765

# Audio Device Index (optional, default: auto-detect)
# Run `python -c "import sounddevice; print(sounddevice.query_devices())"` to list devices
AUDIO_DEVICE_INDEX=auto

# Debug Mode (optional, default: 0)
# Set to 1 for verbose logging
DEBUG=0

# Transcription Settings (optional)
TRANSCRIPTION_LANGUAGE=en
TRANSCRIPTION_MODEL=nova-2

# LLM Settings (optional)
LLM_MODEL=claude-sonnet-4-20250514
LLM_MAX_TOKENS=300
LLM_TEMPERATURE=0.7

# UI Settings (optional)
OVERLAY_OPACITY=0.95
OVERLAY_POSITION=bottom-right
